# Office Presence App

Vue 3 frontend + Node.js/Express backend deployable to Azure.

## Development

```bash
# Frontend
cd frontend && npm install && npm run dev

# Backend
cd backend && npm install && npm run dev
```

## Architecture

See `CLAUDE.md` for architecture and workflow documentation.